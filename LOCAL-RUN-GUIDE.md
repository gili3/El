# 💻 دليل التشغيل المحلي - Eleven Store

نعم، يمكنك تشغيل الموقع محلياً على جهازك الشخصي للاختبار والتطوير. هذا الدليل يوضح لك كيفية القيام بذلك بأفضل طريقة.

---

## 1. متطلبات التشغيل المحلي

بما أن الموقع يعتمد على **JavaScript Modules** و **Firebase**، فإنه لا يمكن تشغيله بمجرد فتح ملف `index.html` مباشرة في المتصفح (بروتوكول `file://`). يجب استخدامه عبر خادم محلي (Local Server).

### الخيارات المتاحة لتشغيل خادم محلي:

#### أ) استخدام إضافة VS Code (الأسهل):
1. افتح المجلد في **Visual Studio Code**.
2. قم بتثبيت إضافة **"Live Server"**.
3. اضغط بزر الفأرة الأيمن على `index.html` واختر **"Open with Live Server"**.

#### ب) استخدام Python (إذا كان مثبتاً):
افتح Terminal في مجلد المشروع وشغل الأمر:
```bash
python -m http.server 8000
```
ثم افتح المتصفح على: `http://localhost:8000`

#### ج) استخدام Node.js (إذا كان مثبتاً):
```bash
npx serve .
```

---

## 2. ما الذي سيعمل وما الذي لن يعمل محلياً؟

| الميزة | الحالة محلياً | السبب |
| :--- | :--- | :--- |
| **واجهة الموقع** | ✅ تعمل بالكامل | ملفات HTML/CSS/JS ستتحمل بشكل طبيعي. |
| **Firebase** | ✅ يعمل | مفاتيح Firebase مدمجة وستتصل بقاعدة البيانات السحابية. |
| **المصادقة (Auth)** | ✅ تعمل | Firebase يدعم `localhost` كطريقة مصادقة افتراضية. |
| **رؤوس الأمان** | ❌ لا تعمل | ملف `.htaccess` يحتاج لخادم **Apache** (مثل XAMPP) ليعمل. |
| **إخفاء المفاتيح** | ❌ لا تعمل | قراءة متغيرات البيئة من `.env` تتطلب خادم Backend أو عملية Build. |

---

## 3. ملاحظات هامة للتشغيل المحلي

### أ) ملفات الأمان:
عند التشغيل المحلي، سيقوم الموقع باستخدام القيم الافتراضية الموجودة في `js/firebase-config.js`. لن يتم تفعيل قيود `.htaccess` لأنها تعمل فقط على خوادم الويب الحقيقية (أو XAMPP/WAMP).

### ب) المصادقة الثنائية (2FA):
ستعمل المصادقة الثنائية بشكل طبيعي محلياً لأنها تعتمد على Firestore لتخزين المفاتيح السرية، طالما أن جهازك متصل بالإنترنت.

### ج) التحقق من النزاهة (Integrity Check):
قد تظهر بعض التحذيرات في Console المتصفح عند التشغيل المحلي بخصوص الهاشات إذا قمت بتعديل الملفات، وهذا طبيعي ويؤكد أن النظام يعمل.

---

## 4. الانتقال من المحلي إلى الإنتاج (Production)

عندما تقرر رفع الموقع على خادم حقيقي:
1. ارفع كافة الملفات الموجودة في الأرشيف.
2. تأكد من وجود ملف `.htaccess` في المجلد الرئيسي لتفعيل رؤوس الأمان.
3. قم بتكوين متغيرات البيئة على الخادم إذا كنت تستخدم نظام استضافة يدعم ذلك (مثل Vercel أو Netlify أو خادم خاص).

---

**نصيحة:** للاختبار الأكثر دقة لرؤوس الأمان، يفضل استخدام برنامج **XAMPP** ووضع الملفات في مجلد `htdocs` لتجربة عمل ملف `.htaccess` محلياً.
