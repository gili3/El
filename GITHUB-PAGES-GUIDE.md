# 🚀 دليل الرفع إلى GitHub Pages - Eleven Store

بما أنك تستخدم **GitHub Pages**، هناك بعض النقاط الهامة التي يجب مراعاتها لأن GitHub Pages لا يدعم ملفات `.htaccess` ويتعامل مع المسارات بطريقة معينة.

---

## 1. خطوات الرفع الصحيحة

1. قم بإنشاء مستودع (Repository) جديد على GitHub.
2. ارفع كافة الملفات الموجودة في الأرشيف المرفق إلى المستودع.
3. اذهب إلى **Settings** > **Pages**.
4. اختر الفرع (Branch) الذي رفعت عليه الملفات (غالباً `main`) واضغط **Save**.

---

## 2. حل مشكلة المسارات (Paths)

إذا كان رابط موقعك هو `https://username.github.io/repository-name/`:
- تأكد أن جميع المسارات في الكود **نسبية** (Relative). لقد قمت بتعديل المسارات الأساسية في هذه النسخة لتكون متوافقة.
- **تنبيه:** إذا واجهت مشكلة في تحميل الصور أو السكريبتات، تأكد من عدم وجود `/` في بداية المسار (مثلاً استخدم `js/main.js` بدلاً من `/js/main.js`).

---

## 3. إعدادات Firebase لـ GitHub Pages

يجب إضافة رابط موقعك على GitHub إلى قائمة النطاقات المسموح بها في Firebase:

1. اذهب إلى **Firebase Console**.
2. اختر مشروعك، ثم اذهب إلى **Authentication** > **Settings** > **Authorized domains**.
3. اضغط **Add domain** وأضف: `username.github.io`.

---

## 4. لماذا قد لا يفتح الموقع على GitHub؟

### أ) ملفات JavaScript Modules:
GitHub Pages يدعمها بشكل ممتاز، ولكن تأكد من رفع مجلد `js` بالكامل وبنفس الهيكلية.

### ب) حالة الأحرف (Case Sensitivity):
GitHub Pages حساس لحالة الأحرف. تأكد أن اسم الملف في الكود يطابق اسم الملف الفعلي تماماً (مثلاً `Style.css` تختلف عن `style.css`).

### ج) ملف 404.html:
لقد أضفت ملف `404.html` في الأرشيف. هذا الملف يساعد GitHub Pages على التعامل مع المسارات الداخلية للموقع بشكل أفضل ومنع ظهور صفحة الخطأ الخاصة بـ GitHub.

---

## 5. الأمان على GitHub Pages

- **رؤوس الأمان:** بما أن GitHub Pages لا يدعم `.htaccess` أو تعديل رؤوس الخادم، فقد قمت بإضافة **Meta Tags** في `index.html` لتعويض جزء من هذه الحماية.
- **المفاتيح:** تذكر أن أي شخص يمكنه رؤية الكود المصدري على GitHub إذا كان المستودع عاماً (Public). يفضل جعل المستودع خاصاً (Private) إذا كنت لا تريد مشاركة الكود.

---

**نصيحة:** بعد الرفع، انتظر دقيقة أو دقيقتين حتى ينتهي GitHub من بناء الموقع (Build)، ثم قم بتحديث الصفحة.
